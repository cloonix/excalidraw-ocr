name: Docker Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: excalidraw-ocr:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Test Python installation
        run: |
          docker run --rm excalidraw-ocr:test python --version
      
      - name: Test cairosvg import
        run: |
          docker run --rm excalidraw-ocr:test python -c "import cairosvg; print('✓ cairosvg OK')"
      
      - name: Test lzstring import
        run: |
          docker run --rm excalidraw-ocr:test python -c "import lzstring; print('✓ lzstring OK')"
      
      - name: Test OCR help command
        run: |
          docker run --rm excalidraw-ocr:test python ocr.py --help
      
      - name: Test Excalidraw OCR help command
        run: |
          docker run --rm excalidraw-ocr:test python excalidraw_ocr.py --help
      
      - name: Run Trivy security scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: excalidraw-ocr:test
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
      
      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
      
      - name: Run Trivy security scan (table output)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: excalidraw-ocr:test
          format: 'table'
          severity: 'CRITICAL,HIGH'
