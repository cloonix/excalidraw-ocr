# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys

# Default model to use for OCR
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4.1

# Alternative: OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# Watcher Configuration
# Stabilization delay: minutes to wait after last file modification before processing
# This prevents processing files that are being actively edited (e.g., during meetings)
# Set to 0 to disable (process immediately)
# Default: 15 minutes
STABILIZATION_DELAY_MINUTES=15

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Advanced: Override paths (usually not needed)
# WATCH_DIR=/input
# PROCESSED_DB_PATH=/input/.excalidraw_processed.json
